# Dev Log

## 2026-07-29 (V2: 3차 - 진짜 알파 재크롭, 화재를 "탄 책 베이스 3단계 + 공용 불꽃 오버레이"로 재설계)

바로 위 항목의 결과물이 실기기에서 제대로 안 보인다는 피드백 두 가지, 그리고 그 과정에서 발견된 사고 하나.

- **크롭 사고 원인 규명**: 지난 크롭이 "이미 프레임을 잘 채우고 있다"고 판단했던 보너스 타일 4장은 사실 오파크 사진이라 크롭 대상이 아니었던 게 아니라, 알파 채널이 전부 불투명(255)이라 크롭 스크립트가 아예 아무것도 못 자른 것이었음(실제로는 좌우 여백이 남아있었음). 거기다 젬(gem)을 "원본으로 되돌린다"며 복사한 백업 파일이, 이번 세션이 아니라 훨씬 이전 세션(뱃지 합성 이전, 단검 이미지였던 시절)의 백업이었던 것으로 드러남 — 백업 스크립트가 "같은 이름의 백업이 이미 있으면 새로 안 만듦" 방식이라 이번 젬(책) 이미지의 백업 차례가 조용히 스킵되고, 복원 시 엉뚱하게 옛날 단검이 되살아난 것. 사용자가 직접 진짜 투명배경(real alpha) PNG로 4장을 재작업해서 넣어줌 — 확인 결과 4장 다 alpha 0.6~0.7 비율, 연결요소 1개로 깨끗함. `scipy.ndimage.label` 연결요소 bbox로 재크롭(패딩만, 정사각형으로 안 맞춰도 `setDisplaySize(56,56)`가 알아서 채움). 백업 파일명에 타임스탬프를 붙이는 방식으로 바꿔서 재발 방지.
- **화재 애니메이션을 "심각도별 3개 애니메이션 세트"에서 "탄 책 베이스 3단계(정적) + 공용 불꽃 오버레이 1개(애니메이션)"로 재설계**: 사용자 제안 — 베이스 이미지 자체가 이미 "1단계=살짝 그을림→3단계=거의 소실"로 심각도를 보여주므로, 불꽃은 심각도별로 새로 안 만들고 하나만 만들어 크기/알파만 심각도에 따라 다르게(`FLAME_OVERLAY_BY_SEVERITY`) 키우기로 함. `createTileAt()`에 `tile.fireOverlay`라는 별도 Sprite 신설(`tile.image` 위, `tile.text`보다 아래 — 타는 동안에도 글자는 계속 보임), `Phaser.BlendModes.ADD`로 설정해 배경이 완벽히 안 지워져도 밝은 불꽃만 도드라지게 함. `igniteTile`/`applyBurnVisual`은 이제 베이스 텍스처를 `burn_stage_N`으로 직접 스왑(정적 이미지 교체)하고 오버레이를 별도로 재생하도록 재작성, `extinguishTile`도 그에 맞게 정리.
- **불꽃 소스를 "불타는 책 사진 영상"에서 "불꽃 테두리 스톡 영상"으로 교체**: `flame_1.mp4`(852×480, 30fps, 300프레임)를 받아보니 검은 레터박스+회색 체커보드(투명 표시 관례) 배경 위에 정사각형 테두리를 따라 넘실거리는 불꽃 — 지난 책 사진 배경보다 색상 대비가 훨씬 뚜렷해서 HSV 채도/명도 임계값(`sat>60` 또는 `val>200`, 최소 밝기 `val>45`)으로 깔끔하게 마스킹됨. 레터박스 제외 콘텐츠 영역을 여러 프레임 샘플링으로 자동 감지 후 6프레임 균등 추출, 마스크를 알파로 쓰고 배경 판정 픽셀은 RGB도 0으로 채워 `flame_1_f1~6.png` 저장 — 일반 알파블렌딩·ADD 블렌드 둘 다에서 깨끗하게 보이는 이중 안전장치.
- **위치 동기화**: `tile.fireOverlay`가 독립된 게임 오브젝트라 `tile.image`가 움직이는 모든 곳(낙하/리필 트윈, 선택 시 바운스 트윈, 매치 제거 팝 애니메이션)에 같이 추가. 단, 착지 스쿼시 트윈(displaySizeY 조작)에는 일부러 안 넣음 — 넣으면 심각도별 오버레이 스케일이 강제로 `tile.baseSize`로 리셋되어버리는 버그가 생기기 때문. `rebuildBoard()`에도 오버레이 정리 로직 추가.
- 이제 안 쓰는 이전 방식의 화재 프레임 18장(`burn_stage{1-3}_{1-6}.png`, 영상에서 추출한 정적 스틸컷 방식)은 `v2/assets/originals/obsolete_burn_frames/`로 이동.

## 2026-07-29 (V2: 실제 애셋 반영 - 보너스 타일 풀텍스처, 화재 프레임을 영상에서 추출, BGM)

계획했던 "미드저니 4장 그리드를 애니메이션 프레임으로 쓰기"가, 각 장이 독립 생성이라 불꽃 모양이 제각각이라 자연스럽게 안 이어진다는 지적을 받고 방식을 바꿈. 계획 파일: `~/.claude/plans/ancient-exploring-bee.md`.

- **보너스 타일 4종 실제 반영**: 사용자가 만든 "금장 보석 표지"/"마법 룬 고서"/"서리 물빛 고서"/"저주받은 마도서" PNG 4장을 `v2/assets/`에 반영. 전부 알파 채널 없는 순수 오파크 사진이라 크로마키 제거는 불필요했고, 다만 여백이 남아있어 `scipy.ndimage.label` 연결요소 크롭을 적용 — 진주/산호/폭탄 3장은 배경과 책이 색상 대비가 뚜렷해서 잘 잘렸지만, 젬은 책의 그림자 진 오른쪽 면이 어두운 배경색과 비슷해서 크롭이 그 부분을 배경으로 오인해 잘라먹는 문제가 발생. 젬만 원본(크롭 없이 이미 프레임을 잘 채우고 있던 상태)으로 되돌림.
- **화재 애니메이션 소스를 정지 이미지 그리드 대신 영상에서 추출로 변경**: 사용자가 미드저니 Animate 기능으로 만든 5.2초짜리 mp4 3개(심각도별)를 전달받음. `opencv-python-headless` 설치해서(시스템 ffmpeg 불필요) 영상당 6프레임을 균등 간격으로 추출하고, 프레임마다 개별 크롭하면 불꽃/연기가 매 프레임 다른 위치까지 번져서 크롭 경계가 흔들리는 문제를 피하기 위해 여러 샘플 프레임의 전경 바운딩박스를 합집합으로 구해 전체 시퀀스에 동일한 크롭을 적용(이번 영상들은 책이 이미 프레임 대부분을 채우고 있어 실질적으로 크롭이 거의 no-op이었음). `burn_stage{1-3}_{1-6}.png` 18장 생성.
- **코드 조정**: `BURN_FRAMES_PER_STAGE = 6` 상수 추가, `preload()`/`create()`의 하드코딩된 프레임 수(4)를 이 상수로 교체 — 6프레임/2fps라 루프가 자연히 2초→3초로 늘어남(요청하신 "2~3초" 범위 안). 나머지 화재 로직(Sprite 전환, 심각도 escalate, 8방향 1칸 확산, 게임오버 조건)은 직전 턴에 이미 구현돼 있어 이번엔 손 안 댐.
- **BGM 신규 연결**: `bgm_1.mp3`(Suno로 제작한 90년대 아케이드 MIDI풍 곡) `preload()`/`create()`에 추가, `BGM_VOLUME=0.25`로 SFX보다 낮게, `loop:true`로 반복 재생. `Phaser.Game` 생성 자체가 "시작" 버튼 클릭 이후에만 일어나는 구조라 브라우저 자동재생 정책과 충돌 없이 바로 재생되고, 기존 음소거 토글이 자동으로 같이 제어함(Phaser Sound Manager의 전역 mute라 별도 처리 불필요).
- 원본 mp4 3개(총 ~18MB)는 게임이 실제로 로드하는 게 아니라 프레임 추출용 소스일 뿐이라, 기존 `assets/originals/` 관례대로 `v2/assets/originals/`(gitignore 대상)에 보관하고 커밋 대상에서 제외.

## 2026-07-29 (V2: 불타는 타일 스프라이트 애니메이션 + 8방향 확산 재설계, 보너스 타일 풀텍스처 전환 준비)

실기기 피드백: 보너스 타일(젬/진주/산호/폭탄)이 우상단 작은 배지로만 표시돼 있어 인지 불가, 불타는 타일도 틴트+깜빡임뿐이라 "불"로 안 읽힘. 계획 파일: `~/.claude/plans/ancient-exploring-bee.md`.

- **보너스 타일 4종 컨셉 확정(애셋 대기 중)**: 금장 보석 표지(젬)/마법 룬 고서(진주)/서리 물빛 고서(산호)/저주받은 마도서(폭탄) — 고대도서관 테마에 맞춘 "특별한 책" 컨셉으로 미드저니 프롬프트 전달. `createTileAt`/`TILE_TEXTURE`가 이미 타입별 텍스처 키 하나만 스왑하는 구조라 애셋 파일만 교체하면 코드 변경 없이 반영됨(뱃지 합성 파이프라인은 폐기).
- **타일을 Image→Sprite로 전환**: `createTileAt()`에서 `this.add.image()`를 `this.add.sprite()`로 교체. 평소엔 기존과 동일하게 정적 텍스처 한 장을 보여주다가, 발화 시 같은 객체에 `.play()`만 호출해서 애니메이션으로 전환 — 객체 destroy/재생성이 없어 포인터 리스너 재등록이나 진행 중 트윈이 깨질 위험 없음.
- **불타는 타일 심각도 3단계 + 스프라이트 애니메이션**: `burnSeverity`(0~3) 필드 신설. `preload()`에 `burn{1~3}_f{1~4}` 12개 프레임 로딩 추가, `create()`에서 심각도별 애니메이션 등록(4프레임/2fps=2초 루프). 프레임 PNG가 아직 없으면(404) 해당 단계 애니메이션 등록을 건너뛰고, `applyBurnVisual()`이 `anims.exists`로 감지해 심각도별 단색 틴트로 자동 폴백 — 에셋 도착 전에도 게임이 안 죽고 최소한의 시각 구분은 됨.
- **확산 메커닉 전면 교체**: 기존엔 매 턴 불타는 타일이 바로 아래 칸과 위치를 스왑하며 "흘러내리다" 맨 아랫줄에 닿으면 게임오버였는데, 이걸 폐기하고 `advanceBurningTiles()`를 재작성 — 1턴을 넘긴(이번 턴에 새로 안 붙은) 불타는 타일마다 심각도를 1단계 올리고, 주변 8칸 중 정상 타일 1개에만 무작위로 옮겨붙게 함(`getAdjacentTiles` 신규). 방금 옮겨붙은 불은 `freshlyIgnited`로 표시해 이번 루프에서 건너뛰어 "1턴 유예" 규칙을 보장. 여러 불이 동시에 타면 각자 매 턴 1칸씩 늘어나 방치 시 실질적으로 매 턴 배로 증가하는 효과라 8칸 중 1칸만 옮겨붙어도 충분히 위협적.
- **게임오버 조건 변경**: "바닥 줄 도달" → **보드 전체(64칸)가 다 불타면**(`burningTiles.length >= GRID_SIZE*GRID_SIZE`)으로 교체. `triggerLibraryBurned()` 메시지도 "서재 전체가 불타버렸습니다"로 수정.
- **리셋 경로 보정**: `rebuildBoard()`(→ `restartGame()`/`triggerFullBoardReset()`가 공유)에서 타일 재활용 시 `tile.image.stop()`을 `setTexture()` 전에 추가 — 애니메이션 재생 중인 Sprite는 stop() 없이 setTexture만 해도 다음 애니메이션 틱에 덮어써지는 문제가 있어서 필요. `burnSeverity` 리셋도 함께 추가.
- `updateBurnVisual()` 완전 삭제, 발화 로직은 `igniteRandomTile()`/`spreadFire`가 공유하는 `igniteTile(tile)` 헬퍼로 통합, `deselectTile()`도 신규 `applyBurnVisual()`을 쓰도록 갱신.

## 2026-07-29 (V2: 갤럭시 폴드 접기/펼치기 화면 잘림 3차 수정 - vh 단위 자체가 문제)

2026-07-28에 다른 세션에서 두 차례(`Phaser.Scale.FIT` 적용, `visualViewport` resize 리스너 추가) 시도했는데도 실기기(갤럭시 폴드)에서 접었다 펼 때 여전히 화면이 잘린다는 재현 보고. 브라우저 캐시 완전 삭제 후에도 동일하게 재현됨.

- **원인 재진단**: 앞선 두 수정 다 "Phaser한테 다시 스케일하라고 알려주는" 것까지만 했지, 정작 캔버스가 맞춰야 할 기준인 `#game-wrap`의 실제 크기가 `height: 100vh`(뷰포트 높이 단위)로 잡혀 있었음. `100vh`는 모바일 브라우저가 URL바 표시/숨김 같은 "브라우저 UI 변화"에는 대개 잘 대응하지만, 폴더블 기기의 접힘/펼침처럼 **OS가 화면 자체를 통째로 바꾸는 상황**에서는 값이 갱신 안 되고 예전 크기로 고정되는 걸로 알려진 고질적 문제가 있음 — `scale.refresh()`를 아무리 호출해도 Phaser가 다시 측정하는 컨테이너 자체가 이미 stale한 크기를 들고 있으면 소용없었던 것.
- **CSS 보강**: `body`/`#game-wrap`의 `100vh`를 그대로 두고 바로 뒤에 `100dvh`(동적 뷰포트 높이 단위, 최신 브라우저 지원)를 추가 선언 — `dvh`를 모르는 구형 브라우저는 그 줄을 무시하고 `vh`가 유지되고, 지원하는 브라우저는 `dvh`가 우선 적용되는 점진적 개선 패턴.
- **JS로 근본 해결**: CSS 단위 자체도 100% 못 믿을 상황이라, `syncGameWrapSize()`를 새로 추가해 `visualViewport.width/height`(또는 폴백으로 `window.innerWidth/Height`)의 실측 픽셀값을 매번 직접 읽어 `#game-wrap`의 `style.width`/`style.height`에 강제로 박아넣은 뒤 `scale.refresh()` 호출 — CSS 뷰포트 단위의 리플로우 타이밍에 전혀 의존하지 않게 됨. `window.resize`/`orientationchange`/`visualViewport.resize` 3개 이벤트 전부에 연결해 커버리지를 넓혔고, 게임 시작 전(이름 입력 오버레이 상태)에도 페이지 로드 시점에 1회 즉시 호출해 초기 접힌/펼친 상태 어느 쪽으로 진입해도 정확한 크기로 시작하게 함.
- 실기기 재현이 안 되는 환경(에뮬레이터 테스트로는 이전 세션에서 이미 재현 실패)이라 코드 검증은 괄호/참조 정합성 스크립트로만 진행 — 실제 폴드 기기에서 재확인 필요.

## 2026-07-25 (V2: 미드저니 애셋 반영 - 크로마키 제거, 배경 5장 체계, 아이콘 배지 합성)

사용자가 미드저니로 V2용 애셋(`tile_normal/gem/pearl/coral/bomb.png`, `mascot.png`, `bg_library_1~5.png`)을 만들어 `v2/assets/`에 넣음. 배경은 10장이 아니라 5장만 제작.

- **배경 티어 5개로 재조정**: `BG_TIER_COUNT` 10→5, `BG_TIER_LEVEL_SPAN` 5→10으로 변경 — 5장 × 10레벨 = `LEVEL_CAP`(50) 전체를 그대로 커버.
- **크로마키 제거 + 재크롭 (연결요소 분석 적용)**: 타일/마스코트 6종은 전부 크림슨/마젠타 배경의 raw 이미지였음. V1에서 얻은 [[feedback_alpha_crop_noise]] 교훈대로, 단순 `getbbox()` 대신 `scipy.ndimage.label`로 연결요소를 구해 가장 큰 덩어리(또는 그 3% 이상 크기의 하위 덩어리들 — 단검처럼 목 부분이 얇아 본체가 두 조각으로 분리되는 경우 대비)만 남기고 나머지 노이즈는 alpha=0으로 제거하는 방식으로 처리. 원본은 `v2/assets/originals/*_raw.png`로 백업(`.gitignore`에 `v2/assets/originals/`도 추가 — 기존 규칙이 `assets/originals/`로만 앵커링돼 있어 v2 쪽엔 안 걸리고 있던 걸 발견해 같이 고침).
- **아이콘류(젬/진주/산호/폭탄) 합성 방식 변경**: 미드저니가 만들어준 그림이 정사각 타일 아트가 아니라 세로로 긴 개별 오브젝트(단검/깃펜+잉크병/유리병/봉인 두루마리)였음. `setDisplaySize(56,56)`로 그대로 쓰면 심하게 찌그러짐 — Phaser에서 별도 아이콘 오브젝트를 타일에 얹어 매 낙하/스왑/선택 애니메이션마다 위치를 같이 옮겨야 하는 방식은 손댈 코드가 너무 많아져서, 대신 **Python으로 미리 합성**하는 쪽을 택함: `tile_normal.png`(가죽 책 표지, 정사각형에 가까움)을 베이스로 깔고, 각 아이콘을 비율 유지한 채 우상단에 반투명 검은 원형 배지 안에 작게 얹어서 완전한 정사각 타일 이미지로 미리 구워냄. 덕분에 `createTileAt()` 등 기존 타일 렌더링/애니메이션 코드는 전혀 손대지 않아도 됨(V1과 동일하게 타입별로 텍스처 하나만 있으면 되는 구조 그대로 유지).
- **효과음 임시 연결**: V2 코드가 처음부터 V1과 같은 파일명 규칙으로 짜여 있어서(`sfx_tile_select_1~3`/`sfx_tile_slide`/`sfx_tile_land`/`sfx_ok~fantastic`), 겹치는 9종은 V1의 mp3를 그대로 복사해 즉시 소리가 나도록 함. 화재 전용 신규 효과음(`sfx_fire_crackle`, `sfx_extinguish`)은 아직 없음 — `playSfx()`의 `cache.audio.exists` 가드 덕에 없어도 게임은 정상 동작, 나중에 전용 효과음 받으면 추가 예정.
- **코드 검증**: 세션 표준 괄호/메서드 참조 정합성 스크립트를 `v2/index.html`에 재적용, 통과.

## 2026-07-25 (2차: 갤럭시 S26 Ultra 등에서 화면 잘림 수정)

위 항목(까만 화면) 배포 직후, 사용자 지인의 갤럭시 S26 Ultra에서는 로딩은 되지만 보드 오른쪽/HUD 일부가 화면 밖으로 잘려 보인다는 제보(사용자 본인 폴드7은 펼침/접힘 둘 다 문제없었음 — 기기별 CSS 뷰포트 폭 차이).

- **원인**: Phaser `config`에 `width:480, height:800`만 있고 `scale` 설정이 없어서, 캔버스가 항상 고정 480×800 픽셀로 생성됨. `#game-wrap`/`#game-container`는 CSS로 반응형(`max-width:480px` 등)이었지만 캔버스 자체는 그 안에서 늘어나거나 줄어들지 않고 그냥 넘쳐서(`body{overflow:hidden}`) 뷰포트보다 캔버스가 넓은 기기에서는 오른쪽/아래가 그대로 잘려 보였음.
- **수정**: `config.scale`에 `mode: Phaser.Scale.FIT, autoCenter: Phaser.Scale.CENTER_BOTH` 추가. 캔버스가 부모 컨테이너 크기에 맞춰 비율 유지하며 자동으로 축소/중앙정렬됨. 브라우저 창을 390×844(뷰포트 폭이 캔버스보다 좁은 경우)로 좁혀 재현 후 수정 확인 — 보드/HUD 전체가 잘림 없이 축소되어 보이고, Scale Manager가 입력 좌표도 자동 보정해줘서 축소된 화면에서 타일 클릭도 정확히 원래 타일에 맞음.

## 2026-07-25 (까만 화면 버그 수정: 애셋 용량 26.75MB→~2MB, BGM 5곡 랜덤 재생 추가)

사용자 본인 및 지인 안드로이드 폰에서 게임 접속 시 까만 화면만 뜨고 아무것도 안 된다는 제보.

- **원인 진단**: `preload()`에서 무조건 불러오는 배경 10장(각 816×1456, RGB PNG) + 타일 9종(각 최대 823×876, RGBA PNG)이 실제 표시 크기(배경 480×800, 타일 56×56)보다 훨씬 큰 원본 해상도를 압축 없이 그대로 쓰고 있어서 합계 **26.75MB**에 달했음. `window.game.scene.scenes[0].load`로 로더 내부 상태를 직접 조회해보니 데스크톱에서도 로딩이 매우 느리게(수 초~수십 초) 진행되다 `create()`가 자동으로 걸리지 않는 경우가 있었고, 저사양 모바일 네트워크/기기에서는 사실상 영구히 로딩이 끝나지 않아 씬이 `LOADING/CREATING` 상태에 멈춰 캔버스가 까맣게 보이는 것으로 결론.
- **애셋 최적화**: PIL로 배경 10장은 720×1200으로 리사이즈 후 JPEG(quality 82)로 재저장(`bg_sea_N.png`→`bg_sea_N.jpg`, 평균 1.5MB→~90KB), 타일 9종은 긴 변 기준 224px로 리사이즈해 RGBA PNG 재저장(평균 1.1MB→~106KB), `dolphin.png`도 1024px→320px로 축소(588KB→67KB). 리사이즈 전 원본은 전부 `assets/originals/*_preoptimize.png`로 로컬 백업(기존 크롭 백업과 동일한 관례, git에는 포함 안 됨). 총 preload 용량 **26.75MB → 약 1.94MB**(약 13배 감소). `index.html`의 배경 로드 경로를 `.png`→`.jpg`로 변경하고, 절대 크기(`setScale`)로 그리던 돌고래를 `setDisplaySize(236,236)`로 바꿔 원본 해상도가 바뀌어도 화면 크기가 그대로 유지되게 함(타일/배경은 원래도 `setDisplaySize` 기반이라 영향 없었음).
- **배경음악(BGM) 5곡 무작위 재생 추가**: `assets/bgm_1~5.mp3`(각 2~3MB)를 사용자가 새로 추가. 용량이 커서 `preload()`에 넣으면 방금 고친 문제가 재발하므로, `create()`에서 보드가 뜬 뒤 `startBackgroundMusic()`으로 5곡 중 무작위 1곡만 별도로(`this.load.audio` + `this.load.start()`) 비동기로 불러와 루프 재생. 기존 음소거 버튼(`this.sound.mute`)이 SFX와 동일하게 자동으로 BGM도 함께 음소거함(Phaser SoundManager 레벨 전역 설정이라 별도 연동 코드 불필요).

## 2026-07-24 (V2 신규 개발: 원작 Bookworm 스타일 "불타는 타일" 게임)

V1을 실제 PopCap 원작 Bookworm/Bookworm Adventures와 비교 조사한 결과를 바탕으로, V1(`index.html`)은 전혀 건드리지 않고 `v2/index.html`에 완전히 새 게임을 만듦. 같은 git 저장소/GitHub Pages/Termux 인프라를 그대로 재사용(`v2/` 경로로 추가 배포).

- **핵심 차별점**: V1은 실시간 카운트다운 타이머 + 레벨별 방해타일 밀도로 압박을 만드는 반면, V2는 원작처럼 **오직 플레이 실력만으로 위험도가 결정**되는 구조로 설계 — 타이머 자체를 완전히 제거.
- **불타는 타일(burning) 시스템**: V1의 얼음/라바/소용돌이/콘크리트 4종을 걷어내고 이 하나로 대체. 매 단어 제출마다 모든 불타는 타일이 한 칸씩 아래로 번지고(`advanceBurningTiles`), 그 단어에 포함된 타일은 소화됨(`extinguishTile`). 3글자(최소 길이) 단어를 만들면 무작위 일반 타일에 새 불씨가 붙음(`igniteRandomTile`). 바닥 줄에 닿으면 즉시 게임오버(`triggerLibraryBurned`). 시각적으로는 별도 애셋 없이 `tile.r` 값(바닥까지 거리)에 따라 노랑→빨강 틴트 보간 + 깜빡임 속도 가속으로 위급함을 표현(`updateBurnVisual`).
- **글자 희귀도 점수**: 스크래블 표준 배점표(`LETTER_VALUES`, Q/Z=10점 ~ 모음=1점)를 추가해 `addedScore = 글자수×50 + 희귀도합×20 + flatBonus`로 점수 공식 교체(V1은 순수 글자수×100). 희귀 글자를 노리는 플레이에 실질적 보상.
- **보너스 타일 재설계**: 젬/진주/폭탄은 V1 그대로, 산호는 "시간 +6초"(V2엔 타이머가 없음) 대신 "단어에 쓰면 불타는 타일 하나 즉시 진화"로 재설계 — 물이 불을 끈다는 테마 매칭. 3/4/5/6/7+ 글자 마일스톤 트리거는 V1과 동일 구조 유지.
- **레벨업 보상 교체**: V1의 "시간 100% 리필" 대신 "현재 불타는 타일 전부 진화"로 교체 — 원작처럼 실력(레벨업)이 곧 안전으로 이어지는 보상.
- **재사용한 V1 패턴 그대로**: 사전 로딩+폴백, 중력 낙하/리필, `ensureSolvable`/`injectGuaranteedWord` 보드 풀이가능 보장, 사운드 안전재생(`playSfx`), 리더보드, 애셋 폴백(`createFallbackTextures`), 구글폼 fire-and-forget 로그 패턴. `localStorage` 키는 V1과 같은 도메인을 공유하므로 전부 `_v2` 접미사로 분리(`bookworm_v2_highscores`, `bookworm_v2_muted`).
- **정리된 부분**: V1에 남아있던 죽은 코드(`pickTileType` 빈 스텁, 쓰이지 않는 `OBSTACLE_UNLOCK` 언락 배열)는 V2엔 아예 없음 — 애초에 그런 개념이 필요 없는 구조라 자연히 깔끔해짐.
- **테마**: 심해/돌고래(V1)와 대비되는 "고서 도서관"(불타는 책·서재) 테마로 전환. 아직 전용 아트 애셋(`tile_*.png`, `mascot.png`, `bg_library_1~10.png`, 신규 sfx 2종)이 없어서 폴백 텍스처로 구동 — 사용자가 미드저니로 제작 예정.
- 코드 검증: 괄호/참조 정합성 스크립트를 `v2/index.html`에도 동일 적용, V1 전용 상수/함수(타이머, 얼음/라바/소용돌이/콘크리트 관련) 잔재가 없는지 grep으로 재확인 완료.

## 2026-07-24 (난이도 완화 + 단어 통계 로그 추가)

여러 판 플레이해도 레벨 15를 못 넘긴다는 피드백. 원인은 레벨업 필요 점수(`levelIncrement`)가 매 레벨 ×1.2 복리로 늘어나는데 제한시간(`maxTime`)은 150초 고정이라, 레벨이 오를수록 "정해진 시간 안에 점점 더 큰 목표"를 벌어야 하는 구조 — 대략 레벨 10~15 이후부터 시간 대비 필요 점수가 급격히 벌어져 진행이 막힘. 방해타일 개수도 5레벨마다 +2개씩 상한 없이 느는 걸 확인했지만(레벨15=8개/64칸), 이번엔 상한을 걸지 않고 시간/점수 쪽만 조정하기로 함(사용자 확인).

- **난이도 완화**: `maxTime` 150→180초, `LEVEL_INCREMENT_MULTIPLIER` 1.2→1.15로 하향. 레벨업마다 100% 리필되는 시간이 그대로 늘어난 `maxTime`을 따라가고, 목표 점수 복리 증가율도 완만해져 고레벨 진입 장벽이 낮아짐. 초반 레벨(`LEVEL_BASE_INCREMENT`=800)은 그대로 유지.
- **단어 통계 로그 추가**: 기존 플레이 로그 구글폼과 별개로, "가장 많이 만든 단어/가장 긴 단어" 분석용 `WORD_FORM_CONFIG` 설정과 `submitWordLog()` 함수 신규 추가. 한 판 동안 만든 단어를 `this.sessionWordLog`에 `{word, length, level, score}`로 계속 쌓아뒀다가, 게임오버 시점(`showGameOverPopup()`, 기존 `submitPlayLog()` 호출 바로 다음)에 단어당 한 줄씩 구글폼으로 일괄 전송(fire-and-forget). 시트에 단어당 1행으로 쌓이므로 COUNTIF/피벗테이블로 최다 사용 단어, 글자수 정렬로 최장 단어를 바로 분석 가능. `WORD_FORM_CONFIG.actionUrl`이 비어있는 동안은 완전히 no-op — 사용자가 새 폼(필드: 이름/단어/글자수/레벨/점수)을 만들어야 실제로 수집 시작.

## 2026-07-23 (10레벨마다 글자 밸런스 리셋 + 보너스 타일 등장 완화)

다른 컴퓨터에서 Claude Code로 이어받아 작업. 레벨 10 즈음부터 "자음/모음 밸런스가 안 맞아서 단어를 만들기 어렵다"는 피드백 조사.

- **원인 진단**: `LETTER_POOL`은 게임 내내 고정 비율(모음 약 42.3%)이라 레벨에 따라 자체적으로 나빠지진 않음(레벨10 보드를 직접 만들어 세어봐도 정상 범위). 실제로는 자주 쓰이는 글자는 계속 소모·리필되는 반면 잘 안 쓰이는 글자(Q/X/Z 등)는 오래 살아남아 보드에 누적되는 게 체감 원인으로 판단. `obstacleTargetForLevel()`이 레벨당 방해타일 목표치를 상한 없이 계속 늘리는 것도 원인 후보로 확인했으나, 방해타일 자체는 문제없다는 판단 하에 이번 작업 범위에서는 제외.
- **`rebalanceLetterTiles()` 추가**: 10/20/30/40 레벨 경계를 넘을 때마다(한 단어로 여러 레벨을 한 번에 넘어도 `Math.floor` 비교로 정확히 1회만) 방해타일/보너스타일은 그대로 두고 일반 타일들의 글자만 `LETTER_POOL`의 실제 모음/자음 비율에 맞춰 다시 뽑아 배정. "🔤 글자 재배치!" 안내 표시 후 `ensureSolvable()`로 재확인.
- **보너스 타일 등장 기준 완화**: 4/5/6/7+글자였던 젬/진주/산호/폭탄 지급 기준을 3/4/5/6글자로 한 단계씩 낮춤. 7글자 이상은 폭탄 대신 새로 만든 `triggerFullBoardReset()`으로 대체 — `rebuildBoard()`를 재사용해 방해타일까지 포함한 보드 전체를 새로 채우는 "완전 리셋" 보상(단, `restartGame()`과 달리 점수/레벨/제한시간/배경 티어는 그대로 유지). 📖 규칙 팝업 텍스트와 `BONUS_MILESTONES` 상수도 새 기준으로 갱신.
- 브라우저에서 `window.game.scene.scenes[0]`으로 씬에 직접 접근해 `rebalanceLetterTiles()`/`triggerFullBoardReset()`을 단독 호출해 검증하고, 실제 클릭으로 "AUD" 3글자 단어를 제출해 젬 보너스 타일이 지급되는 것까지 실제 UI로 확인.

## 2026-07-23 (타일 재크롭 2차: 진짜 원인은 크로마키 노이즈 픽셀)

실기기 스크린샷으로 확인해보니 바로 위 항목의 1차 재크롭(`pad=2`, `alpha.getbbox()` 기반)으로는 타일끼리 여전히 사방으로 떨어져 보였음(둥근 모서리 문제가 아니라 상하좌우 전부). 원인 재조사 결과: `tile_normal.png`/`tile_vortex.png`는 예전 크로마키 배경제거(`dechroma2.py`) 과정에서 남은 아주 작은 노이즈 픽셀(연결 안 된 고립된 opaque 스펙 조각, `tile_normal`은 566개 컴포넌트, `tile_vortex`는 160개)이 이미지 가장자리 여기저기 흩어져 있었고, 이게 `getbbox()`(비영 알파의 단순 바운딩 박스)를 거의 이미지 전체 크기로 왜곡시켜서 정작 진짜 타일 그림(실제 불투명 영역의 99.8~100%를 차지하는 가장 큰 연결 덩어리)은 캔버스 안쪽에 73~140px씩 떨어져 있는데도 "이미 꽉 찬 크롭"으로 잘못 판단됐던 것. `scipy.ndimage.label`로 연결요소 분석 후 가장 큰 덩어리만 남기고(나머지 고립 노이즈 픽셀은 alpha=0으로 제거) 그 덩어리의 실제 바운딩박스로 다시 크롭(`tile_normal.png` 941×1023→756×753, `tile_vortex.png` 903×939→769×811). 나머지 7종은 원래도 단일 덩어리라 문제 없었음. 크롭 전 버전은 `assets/originals/*_precrop3.png`로 백업. 재크롭 후 9종 전부 56px 표시 기준 최대 여백 약 1.3px로 사실상 육안 구분 불가 수준까지 줄임.

## 2026-07-23 (HUD 게이지 박스 오버플로우 수정 + 타일 재크롭)

- **HUD 게이지 바 오버플로우 수정**: 시간 바/레벨 진행도 바가 자신을 감싸는 HUD 프레임보다 길게 그려져(오른쪽 끝이 프레임보다 10px, 음소거/규칙 버튼보다도 5px 더 나가서) 박스와 버튼을 침범하던 버그 수정. 폭 계산 기준을 `gameWidth-60`→`gameWidth-85`로 변경(4곳: 배경 바 2개 생성 코드 + `updateTimerBar`/`updateLevelProgressBar` 각각의 폭 계산)해서 왼쪽 여백(15px)과 대칭으로 프레임 안쪽에 들어오고 버튼과도 20px 간격이 생기도록 정리.
- **타일 밀착 재점검**: "간격 0이 기술적으로 불가능해서 안 한 거냐"는 질문에, 코드상 `tileGap`은 이미 0이라 확인했고 실제 원인은 타일 PNG 애셋 자체에 남은 자잘한 투명 여백(이전 크롭 시 `pad=8`이 그대로 남아있거나, gem/lava/ice는 14~26px 정도 비대칭 여백)이었음. 알파 채널의 실제 불투명 영역만 다시 계산해서 `pad=2`로 9종 타일 PNG를 전부 재크롭(`assets/tile_*.png`) — 크롭 전 버전은 `assets/originals/*_precrop2.png`로 백업. 예: `tile_gem.png` 765×765→718×741, `tile_lava.png` 765×765→739×722. `tile_coral.png`은 이미 여백 0으로 완벽했음.
- **마찰 스파크 depth 고정**: 타일 재크롭과 무관하게 스파크가 항상 타일 위에 보이도록 `startFrictionSparks()`의 파티클 이미터에 `emitter.setDepth(5)` 추가 — 같은 낙하 묶음에서 스파크보다 나중에 리필되는 타일이 이미터를 가리는 엣지 케이스 방지.

## 2026-07-23 (실플레이 피드백 12건: 규칙/방해타일/UX 전면 개편)

계획 파일: `~/.claude/plans/linked-marinating-glacier.md` (plan mode로 사전 승인받고 진행)

- **선택 규칙 자유화 + 보드 9x9→8x8**: 직전 턴에 도입했던 "한 번 방향 고정" 직선 전용 규칙(`lineDir`)을 통째로 제거하고, 마지막 타일과 8방향 인접이면 몇 번이든 계속 꺾어 이어붙일 수 있던 원래 방식으로 복원. 대신 보드를 9x9→8x8로 줄여(`tileDisplaySize` 50→56) 꺾어도 헷갈리지 않게 함.
- **지속적 마찰 스파크**: 낙하 시작 시 1회성 버스트만 나오던 `spawnSparks()`를 `startFrictionSparks(tile, duration)`로 교체 — `emitter.startFollow(tile.image)`로 타일을 따라다니며 낙하 지속시간 내내 좌우 가장자리에서 스파크 방출.
- **얼음 타일 재설계**: 더블탭으로 바로 깨지던 방식을 폐기. 이제 완전히 선택 불가(콘크리트와 동일한 거부 흔들림)이며, 주변 8칸 중 하나가 다른 단어/폭탄으로 제거될 때만 자동으로 깨져 일반 타일로 전환(`breakIceTile`, `removeAndRefill`에서 인접 체크).
- **콘크리트 타일 알파벳 제거**: 어차피 선택 불가한 타일에 글자가 보여 혼란스럽다는 지적으로 `tileText`를 빈 문자열로.
- **라바 지속시간 9초→15초**로 연장.
- **소용돌이 완전 재설계**: 카운트다운-만료-페널티 방식을 폐기하고, 콘크리트처럼 항상 선택 불가하면서 10초마다 무작위 인접 8칸(콘크리트 제외)과 위치를 계속 스왑하며 돌아다니는 "만료 없는 방랑 장애물"로 변경(`registerWanderingVortex`/`tickWanderingVortex`/`moveVortexToNeighbor`). 폭탄 폭발로만 제거 가능.
- **레벨업 연출 경량화**: 팝업+버튼 방식을 폐기하고, 게임을 멈추지 않은 채 화면 중앙에 "LEVEL N!" 큰 텍스트가 1초간(팝인→유지→페이드아웃) 나타났다 사라지는 방식으로 교체(`showLevelUpPopup` 전면 재작성). 시간 100% 회복/배경 티어 전환은 그대로 유지.
- **게임 규칙 버튼 추가**: HUD 우측 상단에 📖 버튼 신설, 클릭 시 선택 규칙/보너스 4종/방해타일 4종/레벨업 효과를 요약한 오버레이 팝업 표시(`createRulesButton`/`createRulesPopup`).
- **보너스타일 무제한 반복 지급**: 4/5/6/7+글자 단어를 완성할 때마다 매번 젬/진주/산호/폭탄을 지급하도록 변경(기존 once-per-game 캡이었던 `bonusGiven` 상태 전부 제거).
- **방해타일 레벨 구간별 자동 주입**: 확률 기반 랜덤 스폰(`pickTileType`)을 폐기하고, 5레벨 구간마다 목표 개수(5~9:4개, 10~14:6개, 15~19:8개... 5레벨마다 +2)를 정해 부족하면 무작위 일반 타일을 방해타일로 전환하는 `ensureObstacleDensity()`를 새로 추가. 보드 생성 직후/매 낙하 완료 후/재시작 후 `ensureSolvable()`과 같은 타이밍에 호출.
- **선택 피드백 교체**: 타일이 작아졌다 커지던 펀치 애니메이션을 제거하고, 타일이 위로 10px 살짝 튀어올랐다 제자리로 돌아오는 단순한 바운스로 교체(크기 변화 없음).
- **플레이 로그 수집 준비**: 게임 종료 시 이름/도달레벨/최종점수/플레이시간을 구글 폼으로 fire-and-forget 전송하는 `submitPlayLog()`/`GOOGLE_FORM_CONFIG`를 추가. 사용자가 아직 폼을 만들지 않아 `actionUrl`이 비어있는 동안은 조용히 건너뜀 — 폼 생성 후 필드 ID를 채워 넣어야 실제로 수집 시작됨.

## 2026-07-22 (슬라이드/착지음 타이밍 수정)

- **슬라이드음이 착지 후에도 계속 들리던 버그 수정**: `sfx_tile_slide.mp3`가 실제로 4.03초짜리 클립인데, 타일은 0.2~1.3초만 떨어지다 보니 타일마다 재생한 소리가 착지(`sfx_tile_land`, 2.59초) 이후에도 한참 겹쳐 울렸음(`mutagen`으로 실제 길이 확인). 타일마다 개별 재생하던 걸 배치(낙하 묶음) 전체에 슬라이드음 하나만 재생하도록 바꾸고, 마지막 타일이 착지하는 순간(`onTileLanded`, 착지음과 같은 타이밍) 120ms 페이드아웃하며 강제로 끊도록 수정 (`startSlideSound`/`stopSlideSound`).
- **낙하 속도 추가 완화**: `animateTileFall()` duration을 `200+distance*1.6`(상한900) → `280+distance*2.2`(상한1300)로 더 늘림.

## 2026-07-22 (사운드 연결, 금속 타일, 낙하 속도, 단어 품질 팝업)

계획 파일: `~/.claude/plans/linked-marinating-glacier.md`

- **사운드 10종 연결**: 사용자가 ElevenLabs로 만든 `sfx_tile_select_1~3`(랜덤 재생)/`sfx_tile_slide`/`sfx_tile_land`/`sfx_ok`/`sfx_good`/`sfx_great`/`sfx_excellent`/`sfx_fantastic`을 `preload()`에 로딩하고 각 시점에 연결. 안전하게 재생하는 `playSfx()` 헬퍼를 만들어 파일이 없거나 로드 실패해도 게임이 죽지 않도록 함(`this.cache.audio.exists()` 체크).
- **볼륨/음소거**: `SFX_VOLUME=0.4`로 전체 음량을 낮게 고정. HUD 우측에 🔊/🔇 토글 버튼 추가, 상태는 `localStorage`(`bookworm_muted`)에 저장돼 다음 방문에도 유지. HUD 박스 폭을 살짝 줄여서 버튼 공간 확보.
- **금속 타일 교체**: 사용자가 미드저니로 만든 브러시드 스틸 타일(`tile_normal.png`)을 배경 제거+크롭해서 반영 (기존 산호 타일처럼 자동 크롭이 잘 됨). 이 과정에서 원래 있던 오래된 sandstone 원본은 `assets/originals/tile_normal.png`에 이미 백업되어 있었고, 새 raw 이미지는 `assets/originals/tile_normal_metal_raw.png`로 별도 보관.
- **타일 글자 대비 개선**: 금속(밝은 회색) 배경에서 기존 흰색 글씨가 잘 안 보인다는 지적으로, 골드색(`#ffcc00`) + 검정 테두리 5px로 변경(기존 흰색+3px에서 강화) — 밝은/어두운 타일 모두에서 대비가 잘 되는 색.
- **낙하 속도 완화**: `animateTileFall()`의 duration 공식을 `140+distance*1.1`(상한650) → `200+distance*1.6`(상한900)로 늘려서 더 천천히 떨어지게 함.
- **타일 완전 밀착 + 마찰 스파크**: `tileGap` 0, `tileDisplaySize` 50으로 재조정(완전히 맞닿음). 낙하 시작 시 Phaser 3.60 파티클 API(`this.add.particles(...).explode()`)로 작은 노란 스파크 버스트를 재생해 "타일이 마찰하며 스파크가 튄다"는 요청 반영.
- **단어 품질 팝업 추가**: 3~7+글자에 따라 "OK → Good → Great! → Excellent!! → Fantastic!!!"를 크기(22→46px)/색상/사운드/이펙트(스파클, 화면 흔들림)가 점점 강해지도록 표시. Google Fonts "Luckiest Guy"(통통 튀는 코믹 디스플레이 폰트)를 로드해서 기존의 밋밋한 sans-serif 대신 사용. `WORD_QUALITY_TIERS` 테이블 하나로 라벨/크기/색/사운드/연출을 관리.

## 2026-07-22 (GitHub Pages 배포)

- **정적 호스팅으로 전환**: 다른 스마트폰(안드로이드)에서도 Termux 없이 접속할 수 있도록, 이 프로젝트를 처음으로 git 저장소로 초기화하고 GitHub(`https://github.com/travelerphs-del/wordgame`)에 푸시. 저장소는 Public이며 로컬 커밋 작성자는 이 저장소에만 로컬로(`git config`, `--global` 아님) `travelerphs-del`/`traveler.phs@gmail.com`으로 설정.
- `.gitignore` 추가: `.vscode/`(SFTP 접속 정보 등 로컬 개발 설정), `.claude/`, `assets/originals/`(크롭 전 백업, 게임에서 미사용) 제외.
- GitHub Pages 활성화는 저장소 Settings에서 수동으로 진행 필요(웹 UI 클릭이라 자동화 불가) — 완료되면 `https://travelerphs-del.github.io/wordgame/`에서 서버/Termux 없이 어떤 기기에서든 접속 가능해짐.
- 이제부터는 로컬 Termux SFTP 동기화와 별개로, 배포하려면 `git add`/`commit`/`push`로 GitHub에 반영해야 함 (Termux는 계속 기존 방식대로 별도 동기화).

## 2026-07-22 (3차: 레벨 리밸런스, 진행도 바, 타일 밀착, 사운드 준비)

계획 파일: `~/.claude/plans/linked-marinating-glacier.md` (plan mode로 사전 승인받고 진행)

- **레벨 상한 100→50, 배경 전환 5레벨 단위**: `LEVEL_CAP=50`, 배경 티어 계산을 `/10`→`/5`로 변경 (50레벨 ÷ 5 = 배경 10장과 정확히 맞아떨어짐).
- **레벨업 목표점수/제한시간 리밸런스**: `LEVEL_BASE_INCREMENT` 400→800(기준값만 2배, 기존 ×1.2 가속 증가율은 유지), `maxTime` 75→150초(2배). 라바 페널티, 단어 제출 시간 보너스 등 다른 시간 수치는 절대값 그대로.
- **레벨업 팝업 2초 자동 닫힘**: `showLevelUpPopup()`에서 `delayedCall(2000, ...)`으로 자동 닫힘·재개. 버튼을 눌러 더 일찍 닫는 것도 여전히 가능(그 경우 자동닫힘 타이머 취소).
- **다음 레벨까지 진행도 바 추가**: HUD를 3단(SCORE/LV → 제한시간 바 → 레벨 진행도 바)으로 재구성하기 위해 HUD 박스 높이를 50→72로 확장. 골드색 바로 현재 레벨 구간 내 진행률 표시.
- **말풍선/HUD 겹침 버그 발견 및 수정**: 지난 턴에 추가한 말풍선(y=55~)이 원래 HUD 박스(y=10~60)와 겹쳐서 말풍선이 뜨는 동안 타이머 바를 가릴 수 있었음. HUD가 커진 김에 말풍선도 35px 아래로 이동해서 해결.
- **타일 간격 최소화**: 사용자가 "타일이 완전히 밀착한 채로 움직이는" 걸 기억한다고 해서 `tileGap` 4→1, `tileDisplaySize` 46→48로 조정(9×48+8×1=440px, 480px 폭에 여유있게 맞음).
- **타일 착지 정착(settle) 애니메이션 추가**: `animateTileFall()` 착지 시점에 `playLandingSettle()`을 호출해 타일이 세로로 살짝 눌렸다가(0.85배) `Back.easeOut`으로 튕겨 복귀 — 사운드 없이도 "턱 걸리는" 물리적 느낌을 줌.
- **사운드 리서치 관련 정정**: 이전 턴에 "PopCap 로고가 금속음"이라는 근거로 사운드 컨셉을 추측했었는데, 이는 텍스트 검색 결과일 뿐 실제 영상/음원을 확인한 게 아니었음(솔직히 시인). Bookworm Adventures 사운드 리소스 페이지(sounds-resource.com)도 403으로 접근 불가했음. 사용자가 직접 기억하는 "타일이 밀착한 채로 움직일 때 나는 긁히는 소리"를 유일한 근거로 삼아 ElevenLabs 프롬프트 3종(선택 클릭 / 타일 이동 시 금속 마찰-긁힘 / 착지 정착음)을 작성해 대화로 전달함. 실제 오디오 파일이 아직 없어서 `preload()`/재생 코드는 연결하지 않음 — 파일 전달받으면 후속 작업으로 진행.

## 2026-07-22 (2차 피드백 반영: 돌고래/말풍선 배치, 대각선, 시드단어 방식 설명)

- **돌고래를 왼쪽으로 이동 + 말풍선 확대**: 돌고래를 화면 중앙(240)에서 왼쪽(100)으로 옮기고, 그만큼 생긴 공간에 말풍선을 훨씬 크게(210×78 → 295×105) 다시 그림. 제목/본문 폰트도 16/12px → 19/14px로 키움. 뜻 설명이 길 경우 고정된 말풍선 크기를 넘지 않도록 120자에서 자르고 "..."을 붙이도록 `truncateDefinition()` 추가.
- **대각선 선택 허용**: 가로/세로만 되던 직선 규칙을 8방향(대각선 포함)으로 확장. `findPossibleWord()`(보드 풀이 가능 여부 체크용)와 `injectGuaranteedWord()`(단어 강제 배치)도 대각선 두 방향까지 포함해서 스캔/배치하도록 `getLinesForDirection()` 헬퍼로 리팩터링.
- **시드 단어 배치 방식 재설명**: `injectGuaranteedWord()`는 "떨어지는 타일이 기존 타일과 조합되도록 낙하를 유도"하는 방식이 아님 — 낙하 애니메이션이 다 끝난 뒤 보드 전체(방금 바뀐 줄이든 아니든 무관)를 스캔해서 유효 단어가 하나도 없으면, 일반 타일로만 이루어진 임의의 3~5칸 직선 구간을 찾아 그 타일들의 글자를 즉시(애니메이션 없이) 통째로 덮어써서 시드 단어를 만드는 방식. 보드 어디에나 나타날 수 있음.

## 2026-07-22 (실기기 테스트 피드백 반영)

- **힌트 기능 완전 제거**: 원인 파악 결과, 힌트가 찾은 타일들을 그냥 다같이 반짝이기만 하고 클릭 순서(방향)를 전혀 안 알려줘서, 직선 선택 규칙과 만나 플레이어가 반대 방향으로 읽으면 `BJE`/`AEI`처럼 존재하지 않는 문자열이 되던 것이 원인이었음(`FTP`/`DBA`는 실제로 외부 5만 단어 사전에 있는 기술 약어였음 - 사전 자체의 노이즈). 근본적으로 방향을 알려주지 않는 한 계속 헷갈릴 수 있어 요청대로 힌트 버튼/로직을 통째로 제거. 버튼 레이아웃은 CLEAR/SUBMIT 2개로 재배치. 산호 타일의 보상 효과는 "힌트 +1"에서 "시간 +6초"로 대체.
- **보드 항상 풀이 가능하도록 보장**: 직선 전용 규칙 + 완전 랜덤 글자 배치 조합이 실제로는 유효한 단어가 거의 안 나오는 판을 자주 만들어냈던 것이 "단어 만들기 너무 어렵다"는 문제의 핵심 원인. `SEED_WORDS`(쉬운 단어 목록)와 `injectGuaranteedWord()`를 추가해서, 보드 생성/재정렬/낙하 완료 시마다 `findPossibleWord()`로 찾을 수 있는 단어가 하나도 없으면 일반 타일 구간에 시드 단어를 강제로 심어 최소 1개는 항상 보장되도록 함.
- **글자풀(LETTER_POOL) 모음 비율 상향**: 기존엔 모음 비율 약 39%였고 Y가 아예 없었음(PLAY, DAY 같은 단어 자체가 불가능했음). 모음 비율을 약 42%로 올리고 Y를 추가.
- **레벨 난이도 완화**: 목표 점수 증가폭을 기존 500(×1.25배)에서 400(×1.2배)로 낮춤 — 직선 전용 규칙으로 단어 찾기가 훨씬 어려워졌는데 기존 곡선은 그걸 감안하지 않은 값이었음. 레벨10 도달까지 필요한 누적 점수가 12,950 → 8,450으로 감소.
- **돌고래 말풍선으로 단어 뜻 표시**: 정답 단어를 제출하면 돌고래 옆에 말풍선이 뜨고, `api.dictionaryapi.dev`(무료 영어 사전 API)로 뜻을 조회해서 영어로 보여줌. 5초 후 자동 페이드아웃. 조회 실패/시간초과(4초 타임아웃) 시 "No definition found." 표시. 연속으로 여러 단어를 빠르게 맞췄을 때 이전 요청 결과가 늦게 도착해 새 단어 위에 덮어써지지 않도록 요청 ID로 최신 것만 반영.

## 2026-07-22

- **타일 이미지 여백 제거**: `assets/tile_*.png` 4종이 실제 그림 주변에 약 15%씩 투명 여백을 포함하고 있어, 코드상 gap(2px)을 줄여도 타일 사이가 멀어 보이는 문제가 있었음. PIL로 여백을 크롭(원본은 `assets/originals/`에 백업)하고 gap을 2→4px로 조정.
- **버튼 눌림 피드백 추가**: CLEAR/HINT/SUBMIT 및 팝업 버튼에 클릭 시 눌리는 스케일 애니메이션이 전혀 없었음. 각 버튼을 Container 기반으로 리팩터링해 `pointerdown` 시 축소→복귀 트윈을 추가.
- **힌트 피드백 강화**: 힌트 성공 시 "힌트 발견!" 플로팅 텍스트 + 타일 노란색 틴트 + 확대 펄스(약 3초)로 강화. 힌트 소진 시에도 안내 문구 추가.
- **글자 크기 확대**: 타일 글자 22→28px, SCORE/LV 16→18px, 선택 단어 표시 22→26px, 버튼 라벨 15→16px.
- **타일 터치 피드백 강조**: 클릭 시 펀치 애니메이션을 더 크게(1.18배 오버슈트, Back.easeOut)하고, 선택된 타일에 지속되는 하늘색 틴트 하이라이트 추가(선택 경로가 눈에 보이도록).
- **정답 제출 시 타일 소멸 + 중력 낙하 리필 구현**: 기존엔 정답을 맞혀도 타일이 전혀 바뀌지 않았음(재사용 무한). 매치된 타일을 팝 애니메이션으로 제거 → 열(column) 단위로 남은 타일이 아래로 슬라이드 → 부족분은 보드 위쪽에서 새 타일이 생성되어 떨어져 채워짐. `Quad.easeIn` 이징으로 점점 가속되는 느낌 구현, 모든 타일 착지 시 `camera.shake()`로 화면 흔들림. 애니메이션 중엔 `isBoardBusy` 플래그로 입력 잠금.
- **레벨업 시간 회복 버그 수정**: 팝업 문구는 "제한시간이 회복됩니다"라고 되어 있었지만 실제 코드에서 `timeLeft`를 갱신하지 않던 버그. `showLevelUpPopup()`에서 `timeLeft = maxTime`으로 설정하도록 수정.
- **플레이어 이름 입력 + 로컬 최고점수 리더보드 추가**: 게임 시작 전 HTML 오버레이(`#name-overlay`)로 이름을 입력받고(엔터/버튼), 사전(dictionary) fetch는 이름 입력을 기다리는 동안 병렬로 미리 시작해 대기 시간을 줄임. `localStorage`(`bookworm_highscores`, 상위 10개)에 이름+점수를 저장하고, 시작 화면과 GAME OVER 팝업에 TOP 5를 표시. 팝업 카드를 더 크게 리사이즈(320×260 → 340×420)하고 설명 텍스트를 위쪽 기준 정렬로 바꿔 가변 길이 콘텐츠(순위표)를 수용. 디버깅 편의를 위해 `window.game`에 Phaser 인스턴스를 노출.

- **Termux SFTP 자동 업로드가 안 되던 문제 해결**: VS Code SFTP 확장이 비밀번호를 저장하지 않아 `uploadOnSave`가 조용히 실패하고 있었던 것으로 추정. 전용 SSH 키(`~/.ssh/termux_bookworm`, 비밀번호 없음)를 생성해 Termux의 `~/.ssh/authorized_keys`에 등록하고, `.vscode/sftp.json`에 `privateKeyPath`를 추가해 이제 비밀번호 없이 자동 업로드되도록 함. 이번 세션의 최신 `index.html`과 `assets/*.png`도 이 키로 직접 수동 업로드해 동기화 완료.

- **Termux 개발 서버를 캐시 방지 서버로 교체**: `python -m http.server`가 캐시 관련 헤더를 안 보내서, 폰 크롬이 예전 `index.html`을 계속 캐시해서 보여주는 문제가 있었음(서버 파일 자체는 항상 최신이었음 — 순수 브라우저 캐시 문제). `~/game/serve_nocache.py`(모든 응답에 `Cache-Control: no-store` 등 추가하는 `SimpleHTTPRequestHandler` 서브클래스)를 만들어 기존 8080 서버 프로세스를 교체. 앞으로는 새로고침하면 항상 최신 파일이 보임.

## 2026-07-22 (특수 타일 + 레벨 디자인)

- **특수 타일 8종 시스템 구현**: 젬(고정보너스)/진주(단어 점수 ×2)/산호(힌트 +1)/폭탄(주변 3×3 파괴 + 대량 보너스, 콘크리트도 부술 수 있는 유일한 수단)/라바(9초 내 못 쓰면 시간 -5초 페널티, 쓰면 오히려 보너스)/소용돌이(9초 내 못 쓰면 주변 타일 글자가 뒤섞임)/아이스(첫 탭은 금만 가고 두 번째 탭에 선택됨)/콘크리트(선택 불가, 폭탄으로만 제거 가능).
- **방해타일 레벨별 언락 + 확률 상승**: 아이스(Lv2) → 라바(Lv3) → 소용돌이(Lv4) → 콘크리트(Lv5) 순으로 언락, 언락 후 레벨당 +2%p씩 등장확률 상승(상한 30%). 콘크리트는 가중치를 낮추고 동시 존재 개수를 최대 2개로 캡.
- **보너스 타일은 랜덤이 아닌 마일스톤 보상**: 4/5/6/7글자 이상 단어를 각각 처음 완성했을 때 딱 1번씩 젬/진주/산호/폭탄을 지급 (레벨 무관). 지급된 타일은 그 단어가 사라지고 새로 채워지는 낙하 타일 중 하나로 등장.
- **목표 점수 증가폭을 ×1.25 배수로 완만하게 가속**: 500 → 650 → 800 → 1000 → ... (기존엔 매 레벨 flat +500). 레벨 100 상한 적용.
- **배경도 레벨 10단계마다 더 깊은 바다로 전환**: `bg_sea_1.png`(햇빛 지대) ~ `bg_sea_10.png`(초심해 해구) 10장을 준비해 레벨업 시 알파 크로스페이드로 전환.
- **타일 글자 가독성 개선**: 어두운 타일(폭탄/소용돌이/라바)에서도 잘 보이도록 타일 글자 색을 진갈색→흰색+검은 테두리로 변경 (HUD 다른 텍스트와 스타일 통일).
- **미드저니로 생성한 신규 타일 에셋 배경 제거**: 진주/소용돌이/콘크리트/폭탄은 마젠타(#FF00FF) 배경을 flood-fill 색상거리 기반으로 자동 투명화 + 바운딩박스 크롭. 산호는 코랄 색상 자체가 마젠타와 색상이 겹쳐 자동 처리가 실패해서, 그리드 오버레이를 그려 좌표를 직접 읽어 수동 크롭함 (`assets/originals/`에 원본 보관).
- 새 에셋(`tile_pearl/vortex/coral/concrete/bomb.png`, `bg_sea_1~10.png`)과 갱신된 `index.html`을 SSH 키로 Termux에 직접 업로드해 동기화. (이번 세션엔 브라우저 프리뷰 도구 연결이 끊겨 실기기/코드 리뷰로만 검증함 — 실제 플레이 테스트 필요.)

## 2026-07-22 (직선 선택 규칙 + 9x9 보드)

- **핵심 규칙 변경 — 단어는 가로/세로 직선으로만**: 기존엔 8방향 아무 인접 타일이나 자유롭게 이어서 선택할 수 있어 너무 쉬웠음. 이제 두 번째 타일을 클릭하는 순간 방향(가로/세로, 시작 방향 무관)이 고정되고, 그 이후로는 같은 직선 위 다음 칸만 선택 가능. 이미 선택된 칸을 다시 클릭하면 그 지점까지 되돌아가고, 직선을 벗어난 칸을 클릭하면 그 타일부터 새로 선택이 시작됨(`this.lineDir`로 방향 추적).
- **보드 크기 8x8 → 9x9로 확대**: 직선 제약으로 어려워진 만큼 칸을 늘려 완화. 타일 크기를 52→46px, 글자 크기를 28→24px로 축소해 9칸이 화면(480px 폭)에 맞게 조정.
- **힌트 탐색도 직선 기준으로 재작성**: 기존 8방향 DFS는 이제 유효하지 않은 경로를 추천할 수 있어서, 가로/세로 각 줄을 콘크리트 타일 기준으로 끊어 3~5글자 연속 구간(정방향/역방향 모두 체크) 중 사전에 있는 단어를 찾는 방식으로 교체.

### 알아둘 것
- Termux에서 게임 서버는 이제 `python ~/game/serve_nocache.py`로 실행함 (기존 `python -m http.server 8080` 대신). 서버가 죽어있으면 Termux에서 `cd ~/game && python serve_nocache.py`로 재시작.
- `.vscode/sftp.json`에 `uploadOnSave: true` + `privateKeyPath`(전용 키)로 Termux(USB 테더링, `172.29.221.157:8022`)에 비밀번호 없이 자동 업로드되도록 설정되어 있음. VS Code + SFTP 확장이 연결된 상태여야 실제로 동작.
- Termux 쪽 `~/game` 폴더에 과거 수동 작업으로 생긴 것으로 보이는 오타 폴더(`asset`, `asstes`)가 남아있음 — 실제 게임은 `assets`(정확한 이름)만 참조하므로 동작엔 문제없지만, 필요하면 나중에 정리 권장.
- 이 저장소는 git 미초기화 상태. 변경 이력은 이 로그와 `assets/originals/` 백업으로만 추적됨.
- 브라우저 프리뷰 도구(Claude 내장)는 프로젝트 폴더 밖의 `file://` 페이지를 완전히 새로고침하지 않는 한계가 있어, 애니메이션 등은 실제 브라우저(또는 Termux)에서 직접 확인 필요.
